## Frontend (static SPA) served by Nginx, with /api proxied to backend service
## Build: docker build -f Dockerfile.frontend -t mindcare-frontend .

FROM nginx:1.27-alpine

COPY docker/nginx.conf /etc/nginx/conf.d/default.conf
COPY src/main/resources/static/ /usr/share/nginx/html/

EXPOSE 80


